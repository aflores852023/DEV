/**
 * Resta dos números.
 *
 * @param {number} a - El minuend.
 * @param {number} b - El subtraendo.
 * @return {number} La diferencia de los dos números.
 */

import React from 'react'
const sumar = (a, b) => {
    return a + b
}

const restar = (a, b) => {
    return a - b
}   
export {restar}
export default sumar